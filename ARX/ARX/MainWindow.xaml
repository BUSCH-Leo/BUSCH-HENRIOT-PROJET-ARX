<Window x:Class="ARX.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ARX" Height="600" Width="600">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Height="301" Width="297">
            <Button Name="JouerButton" Content="Jouer" Width="100" Height="50" Margin="10" Click="JouerButton_Click"/>
            <Button Name="ChargerButton" Content="Charger" Width="100" Height="50" Margin="10" Click="ChargerButton_Click"/>
            <Button Name="QuitterButton" Content="Quitter" Width="100" Height="50" Margin="10" Click="QuitterButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
